<%= form_with(model: [@topic,post]) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form field-group">
    <%= form.text_field :title, class: "form-control", placeholder: "Title of Post" %>
  </div>

  <div class="form field-group">
    <%= form.text_area :body,class: "form-control", placeholdere: "Post content" %>
  </div>
<div class="form field-group">
    <%= form.text_field :tags, value: post.tags.map(&:tag_name).join(","), class: "form-control", placeholder: "Add Tag" %>
    </div>
  <div>
    <%= form.submit %>
  </div>
<% end %>
